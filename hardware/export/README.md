# Exported overlay files

Copy the generated overlay files here after the Vivado build finishes.

Expected names:

- `pynq_z2_accel.bit`
- `pynq_z2_accel.hwh`

These files are ignored by `.gitignore` because they are build outputs.
