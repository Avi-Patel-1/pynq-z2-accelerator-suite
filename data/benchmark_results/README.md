# Benchmark results

`sample_results.json` is generated by the benchmark script with the mock backend.

Use it as a quick sanity check for the Python control flow. Real board-side numbers should be collected again after the overlay is built and loaded on the PYNQ-Z2.
